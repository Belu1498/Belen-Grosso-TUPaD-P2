import java.util.Scanner;

/**
 *
 * @author belen
 */
public class Ejercicio21 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int num1, num2, num3;
        
        System.out.print("Ingrese el primer numero: ");
        num1 = Interger.parseInt(input.nextLine());
        
        System.out.print("Ingrese el segundo numero ");
        num2 = Interger.parseInt(input.nextLine());
        
        System.out.print("Ingrese el tercer numero ");
        num3 = Interger.parseInt(input.nextLine());
        
        if (num1 > num2 && num1 > num3) {
            System.out.print("El mayor es " + num1);
       }
        if (num2 > num1 && num2 > num3) {
            System.out.print("El mayor es " + num2);
        }
        if (num3 > num1 && num3 > num2) {
            System.out.print("El mayor es " + num3);
        }
    }
    
}
